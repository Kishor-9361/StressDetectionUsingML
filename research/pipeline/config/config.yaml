seed: 42
datasets:
  stressid:
    raw_path: "data/stressid"
    modalities: [face, voice, physio]
    physio_signals: [ecg, eda, respiration]
    video_fps: 30
    audio_sr: 16000
    label_granularity: task
  empathicschool:
    raw_path: "data/empathicschool"
    modalities: [face, physio]
    physio_signals: [bvp, eda, hr, temp, acc]
    video_fps: 30
    label_granularity: interval_2min
    nasa_tlx_stress_threshold: 50
  wesad:
    raw_path: "data/wesad"
    modalities: [physio]
    physio_signals: [ecg, eda, emg, respiration, temp, acc, bvp]
    label_granularity: signal
windowing:
  window_sec: 10
  stride_sec: 5
  face_sample_fps: 3
loso:
  n_splits: 5
  group_col: subject_id
training:
  batch_size: 32
  epochs: 80
  patience: 15
  focal_gamma: 2.0
  focal_alpha: 0.25
  adv_lambda: 0.02
gates:
  max_missing_modality_frac: 0.20
  max_fold_std: 0.08
  max_domain_sep_acc: 0.75
  max_leakage_gap: 0.10
  min_face_stress_f1: 0.40
